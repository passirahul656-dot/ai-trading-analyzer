services:
  - type: web
    name: ai-trading-analyzer-v5
    runtime: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: OPENAI_API_KEY
        sync: false
      - key: TWELVE_DATA_API_KEY
        sync: false
